import type { EventListing, MerchandiseItem } from '@/types/content'

// Empty for launch. Add client-approved listings here without changing the
// homepage components. Provider IDs and URLs remain data, not presentation logic.
export const events: readonly EventListing[] = []

export const merchandise: readonly MerchandiseItem[] = []
